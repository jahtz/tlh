import {XmlNode} from '../editor/xmlModel/xmlModel';

interface IProps {
  firstRootNode: XmlNode;
  secondRootNode: XmlNode;
}

export function DocumentMerger({firstRootNode, secondRootNode}: IProps): JSX.Element {
  return <div>TODO!</div>;
}