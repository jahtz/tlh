overwrite: true

schema: "http://localhost:8066/graphql.php"
documents: "src/**/*.graphql"

generates:

  ./schema.graphql:
    plugins:
      - "schema-ast"

  src/graphql.ts:
    config:
      useMembers: true
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
      - "typescript-enum-array"

